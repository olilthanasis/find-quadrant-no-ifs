# include <iostream>
using namespace std;

int quadrant(int x, int y){
	return (!(x>0 && y>0) && (!y>0))*2 + (x>0!=y>0)*1 +1;

}


int main(){
    int x, y, lel;

    scanf("%d", &x);

    scanf("%d", &y);

    lel = quadrant(x,y);

	printf("For x=%d and y=%d, quadrant:%d",x, y, lel);

}
